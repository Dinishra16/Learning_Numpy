#when working with multi dimensional data shape is used
import numpy as np
arr_2d= np.array([[1,2,3],
                  [4,5,6]])
print(arr_2d.shape) #(2,3) is optput because that is the dimension of the array

